project: kintai-app
updated: "2026-05-06"

current:
  branch: feat/gas-performance
  goal: GAS 経由 saveKintai/getMonthlyData の高速化（save 5s→1.5-2s, get warm 4s→50ms）
  status: planned
  done: "前セッションで認証脱 GAS 化完了 + saveKintai bug 修正 + main マージ済み"
  pending:
    - "main から feat/gas-performance ブランチを切る"
    - "GAS/kintai.gs に最適化 A+B+C+D を実装（plan §3 参照）"
    - "GAS デプロイ（VERSION を v12-perf-optim に更新）"
    - "scripts/benchmark-gas-actions.mjs で性能検証"
    - "scripts/audit-auth-migration.mjs で機能 regression なしを確認"
    - "コミット → push → main マージ"
  context: >
    plan: docs/plan-gas-performance-optimization.md
    最適化案 A+B+C+D（書込集約 / formula 既設チェック / 行特定計算優先 / CacheService）。
    E（楽観的更新）と F（Sheets API 直叩き）は不採用、理由は plan §1。
    GAS/kintai.gs のみ編集、フロント・他 .gs ファイル・Netlify Function は触らない。

next:
  - 給与計算機能（作業×時給×時間の自動集計）
  - 作業内容マスタの GAS 経由管理（CSV 直取得からの移行）
  - 楽観的更新（E）の検討（save UX を <200ms 体感に）

history:
  - "2026-05-06 認証脱 GAS 化（auth-login Function + HMAC token）+ saveKintai 引数バグ修正 + URL 一元化"
  - "2026-03-26 GAS 修正・最適化・読書時間短縮効率化（commit ded047c）"
  - "2026-03-26 諸不具合修正（日次入力フォーム初期化など、commit 6506d77）"